/** What the user runs on the workstation to get a pairing code for this device. */
export const PAIR_COMMAND = 'herdr-remote pair';

/** Opens the configuration TUI on the workstation. */
export const TUI_COMMAND = 'herdr-remote';
